---
- hosts: localhost
  roles:
    - install-aws-cli
    - install-eksctl
    - install-helm
    - install-kubectl
    - install-terraform
    - repo-folder-symlink
    - git-configuration
    - bash-aliases
